# Environment
.env

# Docs / planning (hoof-hearted-plan.md holds a PAT -- keep this whole dir out of git)
docs/

# NOTE: app/data/sample_program.json is a committed test fixture, not scratch data --
# this rule is scoped to a root-level data/ dir only so it doesn't also catch app/data/.
/data/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/

# uv
# uv.lock — intentionally committed to pin exact versions for reproducible deploys
# uv.lock

# macOS
.DS_Store
